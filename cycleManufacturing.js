// Wheels, Handle, locks

// Wheels -> tier, wires, rings, spokes
// logics
// Function:
// function -> tell our js that we are going to build a function
// manufactureWheels -> function name
function manufactureWheels(vehicleType) { // function declaration or function definition
    // code
    // How to build wheels
}

// Handle -> steel, police, frame
// logics
// Function:
function createHandle() {
    // Logic
    // Code
}

// Lock -> Keys, shapes
// Logics
// Functions:
function createLock() {
    // Logic
    // Code
}

// Bicycle ->wheels + Handle + 

// Bicycle = Wheels + Handle + lock

const wheels = manufactureWheels(2) // function calling
const handle = createHandle() // function calling
const lock = createLock() // function calling
